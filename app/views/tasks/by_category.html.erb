<h1>
  <%= pluralize @tasks.count, "Task" %>
  <small><%= params[:category] %></small>
</h1>

<%= render 'tasks' %>

<div class="main-button">
  <%= link_to icon_and_text("plus", "Add another task"), new_task_path(category: params[:category]), class: 'btn btn-success btn-lg btn-block' %>
</div>
